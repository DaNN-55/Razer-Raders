services:
  postgres:
    ports: !override
      - "127.0.0.1:5432:5432"

  migrate:
    build: !reset null
    image: razer-raders-live-app:latest

  web:
    build: !reset null
    image: razer-raders-live-app:latest
    ports: !override
      - "127.0.0.1:3004:3000"

  worker:
    build: !reset null
    image: razer-raders-live-app:latest
