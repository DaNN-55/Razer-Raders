# Keep the current product single-instance

Razer-Raders will remain a self-hosted, single-operator tool in the current scope: one instance owns one Radar Profile, its collection history, and Connector Health. A hosted multi-tenant platform, where users separately configure sources and credentials, is a future expansion because it requires explicit isolation, authorization, and scheduling boundaries that do not exist in the present model.
