# Support Ollama and OpenAI-compatible model runtimes

Razer-Raders will support Ollama for local model execution and deployment-configured OpenAI-compatible Chat Completions endpoints for external execution. Both produce the same validated structured assessment; a failed configured runtime causes bounded retries and visible delays rather than an implicit model change.
