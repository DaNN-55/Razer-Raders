# Use a compliant and restricted fetch gateway

All external collection and evidence enrichment in Razer-Raders will use one Fetch Gateway. It prefers official APIs and feeds, parses only explicitly registered public pages in compliance with their access rules, revalidates redirects, blocks private and metadata network destinations, and bounds transport, content type, size, and time; probable leaked credentials are redacted before retention, prompting, or display.
