# Support local models and operator-provided APIs

AI Radar will run with a deployment-selected Model Runtime: a local open model is supported without any required paid API, while an operator may also configure its own external API endpoint and credentials. This preserves the project's free self-hosting promise while allowing deployments to choose a quality, cost, and privacy trade-off.
