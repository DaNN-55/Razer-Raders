# Domain Docs

Before exploring a feature, read the root `CONTEXT.md` and any relevant ADRs in `docs/adr/`.

This is a single-context repository. Use the glossary's canonical vocabulary in issues, specifications, code proposals, and tests. Surface any conflict with an ADR rather than silently overriding it.
